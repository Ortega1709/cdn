server:
  port: 8080
spring:
  data:
    mongodb:
      host: localhost
      username: root
      password: root
      database: cdn
      authentication-database: admin
      port: 27017

  application:
    name: s-cdn

  servlet:
    multipart:
      enabled: true
      file-size-threshold: 20MB
      max-file-size: 200MB
      max-request-size: 200MB
